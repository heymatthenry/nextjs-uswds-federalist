{
  "name": "uswds-nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "federalist:build": "next build && next export",
    "federalist:copy": "mkdir -p _site && rm -rf _site/* && cp -r out/* _site/",
    "federalist": "npm run federalist:build && npm run federalist:copy"
  },
  "dependencies": {
    "next": "10.0.0",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "sass": "^1.27.0",
    "uswds": "^2.9.0"
  }
}
